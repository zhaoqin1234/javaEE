package com.hrxt.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller

public class JSPFordController {

	

	
	/**
	 * 页面跳转配注
	 */
	@RequestMapping("/waterInjection/{page}")
	public String showPage1(@PathVariable String page){
		return "/waterInjection/" + page;
	}
	
	/**
	 * 页面跳转措施
	 */
	@RequestMapping("/measure/{page}")
	public String showPage2(@PathVariable String page){
		return "/measure/" + page;
	}
	
	/**
	 * 页面跳转监测
	 */
	@RequestMapping("/monitor/{page}")
	public String showPage3(@PathVariable String page){
		return "/monitor/" + page;
	}

	

	/**
	 * 页面跳转监测
	 */
	@RequestMapping("/user/{page}")
	public String showPage4(@PathVariable String page){
		return "/user/" + page;
	}
	
	
	/**
	 * 页面跳转监测
	 */
	@RequestMapping("/authority/{page}")
	public String showPage5(@PathVariable String page){
		return "/authority/" + page;
	}

}
